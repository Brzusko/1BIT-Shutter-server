namespace bit_shuter_server.globals.enums {
    public enum PlayerUIScenes {
        MainMenu,
        Lobby,
        LoadingScreen,
        Game
    }

    public enum PlayerGameScenes {
        World,
        None
    }
}